<?xml version="1.0" encoding="utf-8"?>
<!-- For You Section Row -->
<!-- A horizontal scrolling section of personalized recommendations -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginTop="28dp">

    <!-- Section Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="@dimen/screen_padding"
        android:paddingBottom="12dp">

        <!-- Section Icon -->
        <ImageView
            android:id="@+id/section_icon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_sparkle"
            app:tint="@color/simpl_blue" />

        <!-- Section Title -->
        <TextView
            android:id="@+id/section_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:textColor="@color/text_primary"
            android:textSize="18sp"
            android:fontFamily="@font/outfit_semibold"
            tools:text="Because You Watched Breaking Bad" />

        <!-- See All Button (optional) -->
        <TextView
            android:id="@+id/see_all_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="See All"
            android:textColor="@color/simpl_blue"
            android:textSize="13sp"
            android:fontFamily="@font/outfit_medium"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:visibility="gone"
            android:focusable="true"
            android:focusableInTouchMode="true" />

    </LinearLayout>

    <!-- Section Subtitle/Reason -->
    <TextView
        android:id="@+id/section_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/screen_padding"
        android:layout_marginEnd="@dimen/screen_padding"
        android:layout_marginBottom="12dp"
        android:textColor="@color/text_muted"
        android:textSize="13sp"
        android:fontFamily="@font/plus_jakarta_sans_regular"
        android:visibility="gone"
        tools:text="Similar titles and themes"
        tools:visibility="visible" />

    <!-- Horizontal RecyclerView for content cards -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/section_recycler"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="@dimen/screen_padding"
        android:clipToPadding="false"
        android:clipChildren="false"
        android:orientation="horizontal"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

</LinearLayout>
